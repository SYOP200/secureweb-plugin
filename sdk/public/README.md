# SecureWeb SDK

This directory contains the production SDK files for Vercel deployment.

## Files
- `secureweb.js` - The main SDK file (minified, browser-ready)

## Deployment
This directory is served as the public root by Vercel.